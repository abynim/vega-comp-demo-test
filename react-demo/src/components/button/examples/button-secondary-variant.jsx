import { VegaButton } from "@heartlandone/vega-react";

const App = () => {
  return (
    <VegaButton variant={`secondary`} disabled={false} onVegaClick={() => {}}>
      Checkout
    </VegaButton>
  );
};

export default App;
