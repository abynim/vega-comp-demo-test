import { VegaButton } from "@heartlandone/vega-react";

const App = () => {
  return (
    <VegaButton variant={`secondary`} onVegaClick={() => {}}>
      Checkout
    </VegaButton>
  );
};

export default App;
