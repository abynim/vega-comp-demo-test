import { VegaButton } from "@heartlandone/vega-react";

const App = () => {
  return (
    <VegaButton size={`large`} onVegaClick={() => {}}>
      Checkout
    </VegaButton>
  );
};

export default App;
