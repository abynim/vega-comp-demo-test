import { VegaButton } from "@heartlandone/vega-react";

const App = () => {
  return (
    <VegaButton
      variant={`primary`}
      size={`large`}
      disabled={false}
      onVegaClick={() => {}}
    >
      Checkout
    </VegaButton>
  );
};

export default App;
