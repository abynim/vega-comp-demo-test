import { VegaButton } from "@heartlandone/vega-react";

const App = () => {
  return (
    <VegaButton size={`small`} onVegaClick={() => {}}>
      Checkout
    </VegaButton>
  );
};

export default App;
