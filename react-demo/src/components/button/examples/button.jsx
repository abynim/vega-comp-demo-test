import { VegaButton } from "@heartlandone/vega-react";

const App = () => {
  return (
    <VegaButton variant={`primary`} disabled={false} onVegaClick={() => {}}>
      Checkout
    </VegaButton>
  );
};

export default App;
