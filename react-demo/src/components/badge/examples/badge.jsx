import { VegaBadge } from "@heartlandone/vega-react";

const App = () => {
  return (
    <VegaBadge
      bgColor={`bg-accent6-tertiary`}
      textColor={`text-danger-link-visited`}
      text={`Any Text`}
      size={`default`}
    ></VegaBadge>
  );
};

export default App;
